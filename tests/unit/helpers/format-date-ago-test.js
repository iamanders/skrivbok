import {
  formatDateAgo
} from '../../../helpers/format-date-ago';
import { module, test } from 'qunit';

module('FormatDateAgoHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatDateAgo(42);
  assert.ok(result);
});
